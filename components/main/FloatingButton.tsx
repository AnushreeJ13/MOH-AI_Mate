"use client";

import React from 'react';
import Link from 'next/link';

// This component is empty now - we're removing the floating button
const FloatingButton = () => {
  return null; // Return null instead of the button
};

export default FloatingButton;